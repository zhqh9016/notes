package com.taikang.dic.ltci.system.service;

import com.taikang.dic.ltci.portal.api.model.ResultDTO;

public interface MenuService {

  /**
   * 查询用户所拥有菜单
   *
   * @param userId
   * @return
   */
  ResultDTO listMenu(String userId, Integer isMenu);
  /** 查询所有菜单 */
  ResultDTO listMenus();

  ResultDTO listMenuForPortal(String userId);
}
